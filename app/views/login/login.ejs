<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel="stylesheet" href="http://localhost:8000/css/bootstrap.min.css" >

    <title> Login </title>    
</head>
<body>  
    
    <div class="container" style="padding-top:100px">
        
        <div class="col-sm-5 offset-md-3 text-center">
            <h1>Efetuar login</h1>
        </div>

        <br>
        
        <form class="col-sm-5 offset-md-3" action="/login" method="post">
            <div class="form-group">
            <label for="">Usuario</label>
                <input class="form-control" type="text" name="usuario" placeholder="Usuario" value="<%= dados.usuario%>">    
            </div>
            <div class="form-group">
                <label for="">Senha</label>
                <input class="form-control" type="password" name="senha" placeholder="******" value="<%= dados.senha%>"> 
            </div>
            <div class="form-group">
                <button class="btn btn-primary" type="submit">Login</button>
            </div>
        </form>

        <br>

        <% if(erros){ %>
        <ul>
            <% for(var chave in erros){ %>
            <li><%= erros[chave].msg %></li>
            <% } %>
        </ul>
        <% } %>

    </div>
</body>
</html>